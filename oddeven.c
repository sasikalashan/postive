#include<stdio.h>
void main()
{
int num;
clrcsr();
printf("Enter the number:",num);
scanf("%d",&num);
if(num%2=0)
{
printf("The number is even",num)
}
else
{
printf("the nu8mber is odd",num);
}
getch();
}
